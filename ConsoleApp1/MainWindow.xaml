<Window xmlns="https://github.com/avalontm/cmoos"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
		x:Class="MoosApplication.MainWindow"
        Title="Moos Xaml" Height="450" Width="600" >

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Button Grid.Row="0"  Margin="5" Text="Button Fill" Background="Red"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Margin="5" Text="Button Auto" Background="Blue"/>

            <Button Grid.Column="1" x:Name="btnClick" Margin="5" Text="Click me!" Click="onClick" Command="{Binding ElementName=OnCounterClicked}" Foreground="#d2d2d2" Background="Green"/>
        </Grid>
   
    </Grid>
</Window>
